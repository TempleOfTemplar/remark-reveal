import path from "path";

export const revealBasePath = path.resolve(require.resolve('reveal.js'), '..', '..');
export const highlightThemePath = path.resolve(require.resolve('highlight.js'), '..', '..', 'styles');
